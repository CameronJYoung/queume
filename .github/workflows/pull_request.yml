name: Queueme Pull Request

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    name: Cargo Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Project
        uses: actions/checkout@v5

      - name: Test
        run: cargo test
